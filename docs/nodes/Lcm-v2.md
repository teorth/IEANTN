# `Lcm.v2`

**Generated file - do not edit.**  Regenerated by `python scripts/ieantn.py pages`.

> The argument of Lcm.v1 with its threshold abstracted: any Dusart-type prime-gap result at a threshold X0 admitting a lower bound c >= 5 on log X0 gives that lcm(1..n) fails to be highly abundant for every n >= X0^2. Lcm.v1 is the instance at 89693. Stated without imports, so a downstream user supplies whichever prime-gap result they have.

| | |
|---|---|
| Kind | pipeline |
| Status | active |
| Maintainers | Terence Tao |
| Licence | Apache-2.0 |
| Review | self-assessed |

## The source

- **Is the least common multiple sequence lcm(1, 2, ..., n) a subset of the highly abundant numbers?** — Jose Damian Espinosa. MathOverflow, asked 1 October 2025  
  _The question posed the conjecture {L_n} subset HA; the answer is that it is false for all large n. VERIFY BEFORE SUBMISSION: the attribution above is taken from the comment thread, where the argument is Tao's. The authorship of the accepted answer post itself has not been checked, and should be confirmed against the page before this node is used as the basis for any external submission._
- **On highly composite and similar numbers** — Leonidas Alaoglu, Paul Erdos. Trans. Amer. Math. Soc. 56 (1944), 448-469, at pp. 466-467  
  _States that neither n! nor lcm(1, ..., n) can be highly abundant infinitely often, but gives no proof -- an `asserted` claim in this network's vocabulary, not a `literature` one. That assertion is strictly weaker than the conclusion recorded here, which holds for every n >= 89693^2. This node does not depend on it. Recorded because it establishes that the result was expected in the literature long before the MathOverflow question, so no novelty should be claimed for the statement itself._

## Conclusions

### `lcmUpto_not_highlyAbundant_of_primeGap`

**The argument, with its threshold abstracted.**

If some `c ≥ 5` bounds `log X₀` from below, and every `x ≥ X₀` admits a prime in
`(x, x + x/(log x)³]`, then `Lₙ = lcm(1, …, n)` fails to be highly abundant for every `n ≥ X₀²`.

## What `c` is, and where the bound on it comes from

`c` is a lower bound for `log √n`, and the *only* thing the argument uses it for: it bounds the
relative gap `ε = 1/(log √n)³ ≤ 1/c³`. The argument adjoins three primes just above `√n` and
removes three just below `n`, and closes exactly when the resulting comparison of products holds,
which is a numerical condition on `ε` and on `X₀`.

An earlier version of this statement wrote `11.4` here instead. That number is not a property of
the argument at all — it is `log 89693` rounded down, which is to say it came from the threshold
`Lcm.v1` happens to use. Reading the constraint out of the development
(`Solutions/Lcm.v1/LcmDev.lean`) and evaluating it gives a genuine threshold near **`c > 4.12`**,
attained in the worst permitted case `X₀ = eᶜ`; at `X₀ = 89693` it is nearer `3.6`. `5` is that
threshold with margin, chosen so a solver has room and so the constant is not itself a fitted
number. **The margin is deliberate and the bound is not claimed to be sharp.**

## `c` is eliminable, and is kept anyway

Since the conclusion does not mention `c`, this is equivalent to `5 ≤ log X₀ → …`. The parameter
earns its place as documentation rather than logic: it names the quantity the numerics constrain,
so a sharper analysis lowers a stated bound instead of silently changing a magic number, and a
reader can see that `11.4` was never doing the work it appeared to be doing.

The practical gain is the lowered bound, not the parameter: a prime-gap result at any threshold
above `e⁵ ≈ 149` now feeds this node, where `11.4` demanded one above `89000`.

```lean
def lcmUpto_not_highlyAbundant_of_primeGap : Prop :=
  ∀ c X₀ : ℝ, 5 ≤ c → c ≤ Real.log X₀ → IEANTN.HasPrimeInInterval.logPower X₀ 3 →
    ∀ n : ℕ, X₀ ^ 2 ≤ (n : ℝ) → ¬ HighlyAbundant (Nat.lcmUpto n)
```

| | |
|---|---|
| Lean name | `Lcm.v2.lcmUpto_not_highlyAbundant_of_primeGap` |
| Challenge | `Lcm.v2.challenge_lcmUpto_not_highlyAbundant_of_primeGap` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/Lcm/v2/Conclusions.lean#L71) |
| Solution | [`Solutions/Lcm.v2`](https://github.com/teorth/IEANTN/tree/main/Solutions/Lcm.v2) |
| Receipt | [`Lcm.v2.lcmUpto_not_highlyAbundant_of_primeGap.json`](https://github.com/teorth/IEANTN/blob/main/receipts/Lcm.v2.lcmUpto_not_highlyAbundant_of_primeGap.json) |
| Evidence | verified (`lean-comparator`) |
| Sources traced | none |
| Assumes | nothing recorded |
| Assumed by | nothing yet |

**Justification `unjustified`** — none-yet

> A complete, sorry-free solution now exists at Solutions/Lcm.v2/, generalising Solutions/Lcm.v1/LcmDev.lean. It awaits Comparator: the justification stays none-yet until the verification workflow writes a receipt, because a justification an author can set is not evidence. Locally the two guarantees Comparator checks both hold -- the challenge and the solution declare identical types under pp.all, and the solution depends on propext, Classical.choice and Quot.sound alone. Imports: none, and that is the point of this version rather than an unanswered question. The prime-gap hypothesis is internal to the statement, so a user supplies whichever result they have instead of inheriting the one Lcm.v1 depends on.

**Justification `comparator`** — **designated** — lean-comparator

> Comparator accepted the solution. Run: https://github.com/teorth/IEANTN/actions/runs/32817004647

## Limitations

Recorded by the node itself, not derived.

- This node has no justification yet: the implication from Dusart's Proposition 5.4 to the conclusion is claimed but not checked here.
- No novelty is claimed, and no literature search was performed beyond reading the cited MathOverflow thread. See the Alaoglu-Erdos entry.
- The threshold 89693^2 is an artefact of the imported prime-gap result, not of the method.

## How this node was made

- **agent** (Claude Opus 5 (Anthropic)) — Transcribed the conclusion into the network's vocabulary and wrote this metadata, under the direction and review of the maintainer. The Lean proof being referenced was written separately in the PNT+ repository and is not yet ported here.

---

[All nodes](README.md) · [The network](../../GRAPH.md) · [State](../../STATE.md)
