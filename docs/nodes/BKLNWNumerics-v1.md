# `BKLNWNumerics.v1`

**Generated file - do not edit.**  Regenerated by `python scripts/ieantn.py pages`.

> Computation node for Broadbent–Kadiri–Lumley–Ng–Wilk Table 8 (read as Corollary 8.1 reads it) and the Eθ(x) ≤ 1 bound on [2, e^30] that FKS2 Corollary 14 uses. Copied from BKLNW.v1 so the paper node is not also the numerical source. BKLNW.v1 is left unchanged.

| | |
|---|---|
| Kind | computation |
| Status | stub |
| Maintainers | Taksh |
| Licence | Apache-2.0 |
| Review | self-assessed |

## The source

- **Sharper bounds for the Chebyshev function theta(x)** — Samuel Broadbent, Habiba Kadiri, Allysa Lumley, Nathan Ng, Kirsten Wilk. Math. Comp. 90 (2021), no. 331, 2281-2315

## Conclusions

### `table8_psi_bound`

**Table 8**: for consecutive entries `(b, ε)` and `(b', ε')` of the table,
`|ψ(x) − x| ≤ ε · x` for all `e^b ≤ x ≤ e^b'`.

Same reading as `BKLNW.v1.table8_psi_bound` / Corollary 8.1. `zip` with `tail` is
consecutive entries and says nothing about the last row.

```lean
def table8_psi_bound : Prop :=
  ∀ p ∈ table8.zip table8.tail, ∀ x : ℝ,
    Real.exp (p.1.1 : ℝ) ≤ x → x ≤ Real.exp (p.2.1 : ℝ) → |Chebyshev.psi x - x| ≤ p.1.2 * x
```

| | |
|---|---|
| Lean name | `BKLNWNumerics.v1.table8_psi_bound` |
| Challenge | `BKLNWNumerics.v1.challenge_table8_psi_bound` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/BKLNWNumerics/v1/Conclusions.lean#L26) |
| Evidence | computation (`numerical`) |
| Sources traced | traced |
| Assumes | [`Buthe2016.v1.theorem_2_psi`](Buthe2016-v1.md#theorem_2_psi), [`PlattTrudgian2021.v1.theorem_1_numerical`](PlattTrudgian2021-v1.md#theorem_1_numerical) |
| Assumed by | [`BKLNW.v2.table8_psi_bound_above`](BKLNW-v2.md#table8_psi_bound_above) |

**Justification `bklnw-table8`** — **designated** — numerical, Table 8, read as its Corollary 8.1 reads it

> Same statement as BKLNW.v1.table8_psi_bound. The tabulated ε(b) are the output of Buthe 2016 Theorem 2, Platt–Trudgian 2021 Theorem 1, and the algorithm of Buthe 2018 Theorem 1 (not an importable proposition). zip/tail is consecutive rows; the last row has no upper endpoint.

### `theta_error_le_one`

**Tables 13 and 14**, as `FKS2` uses them: `Eθ(x) ≤ 1` for every `2 ≤ x ≤ e³⁰`.

Same as `BKLNW.v1.theta_error_le_one`.

```lean
def theta_error_le_one : Prop :=
  ∀ x : ℝ, 2 ≤ x → x ≤ Real.exp 30 → Eθ x ≤ 1
```

| | |
|---|---|
| Lean name | `BKLNWNumerics.v1.theta_error_le_one` |
| Challenge | `BKLNWNumerics.v1.challenge_theta_error_le_one` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/BKLNWNumerics/v1/Conclusions.lean#L33) |
| Evidence | computation (`numerical`) |
| Sources traced | identified |
| Assumes | [`Buthe.v1.theorem_2_theta_lower`](Buthe-v1.md#theorem_2_theta_lower) |
| Assumed by | nothing yet |

**Justification `bklnw-tables-13-14`** — **designated** — numerical, Tables 13 and 14, as used in FKS2 Corollary 14

> Same statement as BKLNW.v1.theta_error_le_one: Eθ(x) ≤ 1 on [2, e^30]. Transcribed from FKS2's use of those tables, not re-read off the printed tables.

## Limitations

Recorded by the node itself, not derived.

- Table 8 values are taken from the paper, not recomputed.
- theta_error_le_one follows FKS2's reading of Tables 13 and 14.
- BKLNW.v1 still carries the same conclusions; this node is the split requested so a pipeline version can import numerics without editing v1.

## How this node was made

- **manual** — Split the table-driven claims off BKLNW.v1 onto a computation node. Statements copied; no independent numerical verification.

---

[All nodes](README.md) · [The network](../../GRAPH.md) · [State](../../STATE.md)
