# `BKLNW.v2`

**Generated file - do not edit.**  Regenerated by `python scripts/ieantn.py pages`.

> Same paper as BKLNW.v1. This version keeps the analytic ψ−θ bound and states the Table 8 unbounded reading as depending on BKLNWNumerics.v1, so the numerical table is not restated on the paper family.

| | |
|---|---|
| Kind | pipeline |
| Status | awaiting-solution |
| Maintainers | Taksh |
| Licence | Apache-2.0 |
| Review | self-assessed |

## The source

- **Sharper bounds for the Chebyshev function theta(x)** — Samuel Broadbent, Habiba Kadiri, Allysa Lumley, Nathan Ng, Kirsten Wilk. Math. Comp. 90 (2021), no. 331, 2281-2315

## Conclusions

### `corollary_5_1`

**Corollary 5.1**, on the range where its coefficients are explicit.

For `7 ≤ b ≤ 38 log 10` and every `x ≥ e^b`,
`ψ(x) − θ(x) < (1 + 1.93378·10⁻⁸) √x + a₂(b) x^{1/3}`.

```lean
def corollary_5_1 : Prop :=
  ∀ b : ℝ, 7 ≤ b → b ≤ 38 * Real.log 10 → ∀ x ≥ Real.exp b,
    Chebyshev.psi x - Chebyshev.theta x <
      (1 + 1.93378e-8) * x ^ ((1 : ℝ) / 2) + a₂ b * x ^ ((1 : ℝ) / 3)
```

| | |
|---|---|
| Lean name | `BKLNW.v2.corollary_5_1` |
| Challenge | `BKLNW.v2.challenge_corollary_5_1` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/BKLNW/v2/Conclusions.lean#L40) |
| Evidence | cited (`literature`) |
| Sources traced | identified |
| Assumes | [`Buthe.v1.theorem_2_theta_lower`](Buthe-v1.md#theorem_2_theta_lower) |
| Assumed by | nothing yet |

**Justification `bklnw-paper`** — **designated** — literature, Corollary 5.1, equations (2.10) to (2.12), restricted to b <= 38 log 10

> Same statement as BKLNW.v1.corollary_5_1. Imports Buthe's (1.7) as on v1. The Table 8 half of Corollary 2.1 is not an import here; it lives on BKLNWNumerics.v1.

### `table8_psi_bound_above`

**Table 8 tail**, importing the consecutive-interval table from `BKLNWNumerics`.

For every entry `(b, ε)` of Table 8, `|ψ(x) − x| ≤ ε · x` for all `e^b ≤ x ≤ e^25000`.
This is `BKLNW.v1.table8_psi_bound_above`, now a pipeline step from the
consecutive-row claim plus that the tabulated `ε` decrease.

```lean
def table8_psi_bound_above : Prop :=
  ∀ p ∈ BKLNWNumerics.v1.table8, ∀ x : ℝ,
    Real.exp (p.1 : ℝ) ≤ x → x ≤ Real.exp 25000 → |Chebyshev.psi x - x| ≤ p.2 * x
```

| | |
|---|---|
| Lean name | `BKLNW.v2.table8_psi_bound_above` |
| Challenge | `BKLNW.v2.challenge_table8_psi_bound_above` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/BKLNW/v2/Conclusions.lean#L50) |
| Evidence | cited (`literature`) |
| Sources traced | identified |
| Assumes | [`BKLNWNumerics.v1.table8_psi_bound`](BKLNWNumerics-v1.md#table8_psi_bound) |
| Assumed by | nothing yet |

**Justification `from-table8`** — **designated** — literature, Table 8 in the form used by Corollaries 2.1 and 15.1

> Follows from BKLNWNumerics.v1.table8_psi_bound together with the tabulated ε strictly decreasing, checked on v1 across consecutive pairs. Cutoff e^25000 is the last tabulated b. Data is BKLNWNumerics.v1.table8, not a second Tables.lean on this version.

## Limitations

Recorded by the node itself, not derived.

- corollary_5_1 is still only the b ≤ 38 log 10 branch.
- BKLNW.v1 is not deprecated; it remains the paper-faithful node.
- No Lean proof is claimed.

## How this node was made

- **manual** — Branched v2 so the Table 8 tail imports the computation node instead of duplicating the table on this family.

---

[All nodes](README.md) · [The network](../../GRAPH.md) · [State](../../STATE.md)
