# `PlattZeroSum.v1`

**Generated file - do not edit.**  Regenerated by `python scripts/ieantn.py pages`.

> The bound 2 * sum over non-trivial zeroes rho of zeta with 0 < Im rho <= 20000, counted with multiplicity, of 1/Im rho, is at most 10.3194. The value 10.319317... is computed by Chirre and Helfgott from the ordinates of all zeroes up to that height, supplied by D. Platt, and used in their bound on the weighted sum over zeroes that gives the square-root term of their explicit psi estimate. It is a finite numerical fact that zero-counting estimates cannot replace at the precision needed, and that a Lean proof cannot reach, so it is recorded once as its own node.

| | |
|---|---|
| Kind | standard |
| Status | active |
| Maintainers | Terence Tao |
| Licence | Apache-2.0 |
| Review | self-assessed |

## The source

- **Optimal bounds for sums of non-negative arithmetic functions** — Andres Chirre, Harald Andres Helfgott. CH2  
  _arXiv:2512.15709. The value appears in the proof of Proposition prop:vihuela; the zero ordinates were supplied to the authors by D. Platt._

## Conclusions

### `inv_ordinate_sum_le`

**The reciprocal-ordinate sum up to `2·10⁴`.** Counting non-trivial zeroes `ρ` of `ζ` with
multiplicity `m(ρ)`,

`2 ∑_{0 < Im ρ ≤ 20000} m(ρ)/Im ρ ≤ 10.3194`.

The index set is `zetaZeroesIn Set.univ (Set.Ioc 0 20000)`: every zero with positive imaginary part
is non-trivial, so no restriction on the real part is needed. The height `20000` is included,
matching the paper's `γ ≤ t₀`.

```lean
def inv_ordinate_sum_le : Prop :=
  2 * zetaZeroesSum Set.univ (Set.Ioc 0 20000) (fun ρ : ℂ ↦ 1 / ρ.im) ≤ (10.3194 : ℝ)
```

| | |
|---|---|
| Lean name | `PlattZeroSum.v1.inv_ordinate_sum_le` |
| Challenge | `PlattZeroSum.v1.challenge_inv_ordinate_sum_le` |
| Source | [Conclusions.lean](https://github.com/teorth/IEANTN/blob/main/IEANTN/Nodes/PlattZeroSum/v1/Conclusions.lean#L53) |
| Evidence | cited (`literature`) |
| Sources traced | none |
| Assumes | nothing recorded |
| Assumed by | [`CH2.v1.corollary_1_2_lambda_sum`](CH2-v1.md#corollary_1_2_lambda_sum), [`CH2.v1.corollary_1_2_psi`](CH2-v1.md#corollary_1_2_psi) |

**Justification `ch2-computation`** — **designated** — literature, proof of Proposition prop:vihuela (section 7, 'Sums over non-trivial zeros')

> The paper states 2 sum_{0 < gamma <= t_0} 1/gamma = 10.319317... = (1/2 pi) log^2(t_0/2 pi) - 0.03435... with t_0 = 2 * 10^4, "by a brief computation using the location of all rho with gamma <= t_0 (furnished by D. Platt)". This node asserts the upper bound on that authority, rounded up to 10.3194. A CONSISTENCY CHECK, NOT A VERIFICATION: (1/2 pi) log^2(20000/2 pi) = 10.353677..., and the paper's own decomposition gives 10.353677 - 0.03435 = 10.31933, agreeing with the printed 10.319317 to the rounding of the 0.03435... That confirms the transcription, not the computation. MULTIPLICITY: the paper sums over zeroes with multiplicity (its section 7 says so explicitly); zetaZeroesSum weights by zetaOrder, which agrees.

## Limitations

Recorded by the node itself, not derived.

- THE JUSTIFICATION IS THE LITERATURE, NOT A COMPUTATION RUN HERE. The value is the paper's, from data supplied by Platt; nothing in this repository recomputes it. A computation node checking it against published zero tables with rigorous error bounds would upgrade it.
- The tsum in zetaZeroesSum would be 0 if the family were not summable. It is summable (finitely many zeroes below the height), but that is a separate theorem, so the statement read on its own is only meaningful given it.
- No novelty is claimed.

## How this node was made

- **agent** (Claude Opus 5 (Anthropic)) — Identified the numerical input while planning section 7 of CH2.v1, checked that no zero-counting estimate can substitute at the required precision, and wrote the statement and this metadata, under the direction of the maintainer.

---

[All nodes](README.md) · [The network](../../GRAPH.md) · [State](../../STATE.md)
